1 - Executar o seguitne comando ap�s o docker-compose up -d:
	docker-compose exec apache bash -c "apt update && apt -y upgrade && apt install -y libapache2-mod-php php-mysql php-pear php-fpm php-dev php-zip php-curl php-xmlrpc php-gd php-mbstring php-xml"

2 - Comando: docker-compose exec db bash, instalar vim ou nano (apt update && apt install ...) e adicionar o default authentication plugin em /etc/mysql/my.cnf
    [mysqld]
    default_authentication_plugin = mysql_native_password

3 - Sair do bash do container bd e usar comando: docker-compose restart;

4 - Abrir o site pelo localhost:5000 e instalar o Wordpress;

5 - No painel do Wordpress, ir em Ferramentas, Importar (Importar Wordpress) e importar o arquivo .xml mais recente da pasta /scripts/bkp

6 - Apar�ncia -> Temas -> Ativar Patus;

7 - Excluir P�gina Exemplo;

8 - Configura��es -> Links Permanentes -> Nome do Post

9 - Ativar o plugin PageRestrict, modificar a mesnagem padrão e ativá-lo na página "Gerenciar" 

Obs.: 
	- Verde padr�o do tema: #7fc732